name: glc
priority: A
repo_key: glc
repo_path: external/repos/GLC
pretrained_dir: external/pretrained/GLC
usage_policy:
  core_integration_allowed: false
  mvp_note: Baseline/reference only; no default CoSER semantic VQ initialization.
evaluation:
  actual_bpp: verify
  datasets: [kodak, clic_valid, div2k]

